diff --git a/src/apps/cli/adapters/NodeFileSystemAdapter.ts b/src/apps/cli/adapters/NodeFileSystemAdapter.ts
index b90ad73..cfe66be 100644
--- a/src/apps/cli/adapters/NodeFileSystemAdapter.ts
+++ b/src/apps/cli/adapters/NodeFileSystemAdapter.ts
@@ -126,6 +126,12 @@ export class NodeFileSystemAdapter implements IFileSystemAdapter<NodeFile, NodeF
             const entries = await fs.readdir(fullPath, { withFileTypes: true });
 
             for (const entry of entries) {
+                // Skip hidden/dot directories (e.g. .livesync/) — they contain
+                // internal LevelDB files that must never be synced and whose
+                // contents change at any time (compaction races → ENOENT).
+                if (entry.isDirectory() && entry.name.startsWith(".")) {
+                    continue;
+                }
                 const entryRelativePath = path.join(relativePath, entry.name).replace(/\\/g, "/");
 
                 if (entry.isDirectory()) {
diff --git a/src/apps/cli/commands/runCommand.ts b/src/apps/cli/commands/runCommand.ts
index 12a315b..bd35efa 100644
--- a/src/apps/cli/commands/runCommand.ts
+++ b/src/apps/cli/commands/runCommand.ts
@@ -20,7 +20,7 @@ export async function runCommand(options: CLIOptions, context: CLICommandContext
 
     if (options.command === "sync") {
         console.log("[Command] sync");
-        const result = await core.services.replication.replicate(true);
+        const result = await core.services.replication.replicateAllFromRemote(true);
         if (!result) {
             // TODO: Standardise the logic for identifying the cause of replication
             // failure so that every reason (locked DB, version mismatch, network
@@ -36,6 +36,26 @@ export async function runCommand(options: CLIOptions, context: CLICommandContext
         return !!result;
     }
 
+    if (options.command === "connection-info") {
+        // Output decrypted CouchDB connection details as JSON.
+        // Used by livesync-watch.js to avoid re-implementing settings decryption.
+        const s = core.services.setting.currentSettings();
+        const info = {
+            couchDB_URI: s.couchDB_URI,
+            couchDB_USER: s.couchDB_USER,
+            couchDB_PASSWORD: s.couchDB_PASSWORD,
+            couchDB_DBNAME: s.couchDB_DBNAME,
+        };
+        process.stdout.write(JSON.stringify(info) + "\n");
+        return true;
+    }
+
+    if (options.command === "sync-push") {
+        console.log("[Command] sync-push");
+        const result = await core.services.replication.replicateAllToRemote(true);
+        return !!result;
+    }
+
     if (options.command === "p2p-peers") {
         if (options.commandArgs.length < 1) {
             throw new Error("p2p-peers requires one argument: <timeout>");
diff --git a/src/apps/cli/commands/types.ts b/src/apps/cli/commands/types.ts
index 01ea118..3e9757d 100644
--- a/src/apps/cli/commands/types.ts
+++ b/src/apps/cli/commands/types.ts
@@ -4,6 +4,8 @@ import { ServiceContext } from "@lib/services/base/ServiceBase";
 export type CLICommand =
     | "daemon"
     | "sync"
+    | "sync-push"
+    | "connection-info"
     | "p2p-peers"
     | "p2p-sync"
     | "p2p-host"
@@ -39,6 +41,8 @@ export interface CLICommandContext {
 
 export const VALID_COMMANDS = new Set([
     "sync",
+    "sync-push",
+    "connection-info",
     "p2p-peers",
     "p2p-sync",
     "p2p-host",
diff --git a/src/apps/cli/main.ts b/src/apps/cli/main.ts
index ecd8afa..10c8e68 100644
--- a/src/apps/cli/main.ts
+++ b/src/apps/cli/main.ts
@@ -275,9 +275,8 @@ export async function main() {
         }
         console.error(`${prefix} ${message}`);
     });
-    // Prevent replication result to be processed automatically.
+    // Prevent replication result from being processed automatically in CLI mode.
     serviceHubInstance.replication.processSynchroniseResult.addHandler(async () => {
-        console.error(`[Info] Replication result received, but not processed automatically in CLI mode.`);
         return await Promise.resolve(true);
     }, -100);
 
diff --git a/src/apps/cli/services/NodeServiceHub.ts b/src/apps/cli/services/NodeServiceHub.ts
index 9815f42..c01f01a 100644
--- a/src/apps/cli/services/NodeServiceHub.ts
+++ b/src/apps/cli/services/NodeServiceHub.ts
@@ -62,12 +62,18 @@ class NodeDatabaseService<T extends NodeServiceContext> extends DatabaseService<
         name: string,
         options: PouchDB.Configuration.DatabaseConfiguration
     ): { name: string; options: PouchDB.Configuration.DatabaseConfiguration } {
+        // Store the local PouchDB inside .livesync/ so it is excluded by
+        // the dotfile filter and never accidentally mirrored to CouchDB.
         const optionPass = {
             ...options,
-            prefix: this.context.vaultPath + nodePath.sep,
+            prefix: nodePath.join(this.context.vaultPath, ".livesync") + nodePath.sep,
         };
         const passSettings = { ...settings, useIndexedDBAdapter: false };
-        return super.modifyDatabaseOptions(passSettings, name, optionPass);
+        // Sanitize the db name: path separators in additionalSuffixOfDatabaseName
+        // (e.g. imported from an Android settings.json) would make LevelDB look
+        // for subdirectories that do not exist → "IO error: .../LOCK".
+        const safeName = name.replace(/[/\\]/g, "-");
+        return super.modifyDatabaseOptions(passSettings, safeName, optionPass);
     }
 }
 class NodeUIService<T extends ServiceContext> extends UIService<T> {
